import { Container, Row, Col } from "reactstrap";

const ServiceConstructor = ({ children }) => (
  <Container className="construct-container">{children}</Container>
);

export default ServiceConstructor;
