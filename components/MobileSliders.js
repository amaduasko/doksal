import { Row } from "reactstrap";
import "./MobileSliders.css";

export const ServiceMobileSlider = ({ children }) => {
  return <Row className="service-mobile-slider-box">{children}</Row>;
};

export default ServiceMobileSlider;
